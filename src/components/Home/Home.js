import React from "react";

import Card from "../UI/Card/Card";
import "./Home.css";

const Home = (props) => {
    return (
        <Card className="home">
            <h1>Welcome To Prepbytes</h1>
        </Card>
    );
};

export default Home;
